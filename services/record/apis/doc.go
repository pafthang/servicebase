// Package apis contains the record service HTTP transport binding layer.
//
// The module now owns generic record CRUD route registration, while the
// top-level API package still hosts the shared handler implementations during
// the transition.
package apis
