// Package apis contains the team service HTTP transport binding layer.
//
// The module now owns `/teams` route registration and team membership helper
// handlers, while the top-level API package only adapts shared dependencies.
package apis
