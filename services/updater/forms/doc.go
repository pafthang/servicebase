// Package forms contains updater service request/submit forms.
//
// The updater service is currently CLI-only, so this package is intentionally
// small. Add validation DTOs here if HTTP/API update flows are introduced.
package forms
