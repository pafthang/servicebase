// Package apis contains the user service HTTP transport binding layer.
//
// The module now owns `/users` route registration and user-scoped helper
// handlers, while shared auth handlers are still delegated from the top-level
// API package during the transition.
package apis
